{
  "name": "offline_chatbot_app",
  "version": "1.0.0",
  "description": "MedicoAI is a cross-platform Flutter application that runs a Large Language Model **completely on-device**.   It offers an AI-powered chat experience **without requiring an internet connection**, making it ideal for private or offline scenarios (e.g. remote clinics, airplanes, or poor-connectivity environments).",
  "main": "index.js",
  "directories": {
    "lib": "lib",
    "test": "test"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build:web": "flutter build web",
    "serve:firebase": "firebase emulators:start --only hosting",
    "build:web:serve": "flutter build web && firebase emulators:start --only hosting",
    "deploy:production": "flutter build web && firebase deploy --only hosting",
    "deploy:preview": "flutter build web && firebase hosting:channel:deploy preview",
    "deploy:preview:pr": "flutter build web && firebase hosting:channel:deploy pr-$(git rev-parse --short HEAD || echo 'manual')",
    "deploy:firestore:rules": "firebase deploy --only firestore:rules",
    "deploy:firestore:indexes": "firebase deploy --only firestore:indexes",
    "deploy:firestore:all": "firebase deploy --only firestore"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@mediapipe/tasks-genai": "^0.10.25"
  },
  "devDependencies": {
    "firebase-tools": "^13.28.0"
  },
  "overrides": {
    "js-yaml": "^4.1.1"
  }
}
